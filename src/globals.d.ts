// Constantes que inyecta Vite en tiempo de build (ver `define` en vite.config.ts).

/** Versión de la app: fecha del último commit + hash corto, ej. `2026.08.08·85803d8`. */
declare const __APP_VERSION__: string
